  # TODO: Belong in a Unit test
  # it "sets helpful accessors" do
  #   expect( evaluator.report ).to eq(report)
  #   expect( evaluator.ruleset ).to eq(ruleset)
  # end

  # TODO: Belong in a Unit test
  # it "sets a #results accessor with a list of rule => raw results hashes" do
  #   expect( evaluator.results.first ).to include(rule => {foo: "bar"})
  # end
